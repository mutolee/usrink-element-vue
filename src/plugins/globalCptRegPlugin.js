/**
 * 全局组件注册管理插件，所有的全局组件都应该在此注册
 * <p>
 * 文档地址：https://cn.vuejs.org/guide/components/registration.html
 * @param app 根实例对象
 */
const globalCptRegPlugin = (app) => {
    // app.component('MyComponent', MyComponent)
}

export default globalCptRegPlugin